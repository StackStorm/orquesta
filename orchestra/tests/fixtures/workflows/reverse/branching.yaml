version: '2.0'

branching:
    description: A basic workflow that demonstrate branching.
    type: reverse
    tasks:
        task1:
            action: std.noop

        # branch 1
        task2:
            action: std.noop
            requires: [task1]
        task3:
            action: std.noop
            requires: [task2]

        # branch 2
        task4:
            action: std.noop
            requires: [task1]
        task5:
            action: std.noop
            requires: [task4]
