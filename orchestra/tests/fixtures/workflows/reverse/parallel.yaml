version: '2.0'

parallel:
    description: A sample set of parallel sequential workflows.
    type: reverse
    tasks:
        # Branch 1
        task1:
            action: std.noop
        task2:
            action: std.noop
            requires: [task1]
        task3:
            action: std.noop
            requires: [task2]

        # Branch 2
        task4:
            action: std.noop
        task5:
            action: std.noop
            requires: [task4]
        task6:
            action: std.noop
            requires: [task5]
