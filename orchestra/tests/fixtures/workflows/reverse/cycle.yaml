version: '2.0'

cycle:
    description: A sample workflow with a cycle.
    type: reverse
    input:
        - count: 0
    tasks:
        prep:
            action: std.noop
        task1:
            action: std.noop
            requires: [prep, task3]
        task2:
            action: std.noop
            requires: [task1]
        task3:
            action: std.noop
            requires: [task2]
