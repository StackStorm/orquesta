version: '2.0'

sequential:
    description: A basic sequential workflow.
    type: reverse
    tasks:
        task1:
            action: std.noop
        task2:
            action: std.noop
            requires: [task1]
        task3:
            action: std.noop
            requires: [task2]
