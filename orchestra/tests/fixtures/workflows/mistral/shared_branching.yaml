version: '2.0'

shared_branching:
    description: A basic workflow that demonstrate branching with a shared task.
    type: direct
    tasks:
        task1:
            action: core.noop
            on-success:
                - task2
                - task3

        # branch 1
        task2:
            action: core.noop
            on-success:
                - task4

        # branch 2
        task3:
            action: core.noop
            on-success:
                - task4

        task4:
            action: core.noop
            on-success:
                - task5
        task5:
            action: core.noop
            on-success:
                - task6
        task6:
            action: core.noop
