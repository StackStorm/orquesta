version: '2.0'

branching:
    description: A basic workflow that demonstrate branching.
    type: direct
    tasks:
        task1:
            action: core.noop
            on-success:
                - task2
                - task4

        # branch 1
        task2:
            action: core.noop
            on-success:
                - task3
        task3:
            action: core.noop

        # branch 2
        task4:
            action: core.noop
            on-success:
                - task5
        task5:
            action: core.noop
